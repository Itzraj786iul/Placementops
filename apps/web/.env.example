NEXT_PUBLIC_API_URL=http://localhost:8000
# Used by Next.js server routes / BFF proxy (set to Render URL in staging/production).
INTERNAL_API_URL=http://localhost:8000
# Dev-only email/password login UI. Leave unset/false in staging and production.
NEXT_PUBLIC_ENABLE_DEV_LOGIN=false
